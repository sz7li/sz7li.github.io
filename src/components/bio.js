import React from 'react'

const Bio = (props) => {
    return (
        <>
        <nav className={`relative container mx-auto p-6 ${props.bg.primary.bg_2}`}>
            <h1>Bio</h1>
        </nav>
        
        </>
        
    )
}

export default Bio